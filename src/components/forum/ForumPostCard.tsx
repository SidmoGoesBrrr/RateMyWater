// TODO: Pass 2 — extract the card from forum/page.tsx into this file once
// more behavior (rating row, comment expand/collapse animations, tier tint)
// lands. For Pass 1 the card is implemented inline in forum/page.tsx.
export default function ForumPostCard() {
  return null;
}
