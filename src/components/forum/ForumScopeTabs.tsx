// TODO: Pass 2 — Global / Around-me segmented control with geolocation
// permission state handling. Coordinates come from navigator.geolocation
// once, then passed to /api/forum as lat/lng/radiusKm.
export default function ForumScopeTabs() {
  return null;
}
