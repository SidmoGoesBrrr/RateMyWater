// TODO: Pass 2 — horizontal 5-emoji rating row using RATING_META from
// src/lib/water-types.ts. Optimistic update, ripple animation on tap,
// redirect to /auth/login?returnTo=/forum when unauthenticated.
export default function ForumRatingRow() {
  return null;
}
