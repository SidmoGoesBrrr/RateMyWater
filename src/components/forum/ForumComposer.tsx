// TODO: Pass 3 — extract composer from forum/page.tsx, add Google Maps
// pin-drop for location attach. Reuses useGoogleMaps() from
// src/lib/google-maps.ts and the dark MAP_STYLES from GoogleMapView.tsx.
export default function ForumComposer() {
  return null;
}
